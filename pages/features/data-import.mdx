# Data import
import Image from 'next/image';
import EditorImage from './data.png';

<Image style={{width:'100%',marginTop:'20px',borderRadius:'10px'}} src={EditorImage} />
For dynamic label generation you need data, isn’t it? We’ve made a data upload system that supports XLS and CSV format files. You can import data from external public API. You can also import Woocommerce products and order data for your dynamic label generation. Learn more about data import in the Data section.